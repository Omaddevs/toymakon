# Ishlab chiqishda bo'sh qoldiring — Vite `/api` ni 127.0.0.1:8000 ga proxylaydi.
# Production build uchun to'liq API manzili (oxirida / bo'lmasin),
# masalan backend alohida serverda bo'lsa:
# VITE_API_BASE_URL=https://api.sizning-domen.uz

# Backend (MySQL + Django): toymakon-backend/README.md
